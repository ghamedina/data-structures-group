
#include <string.h>
#include "BSTIterator.h"


/*
int main(int argc, const char * argv[]) {
 
    struct BinarySearchTree tree;
    struct BSTIterator iter;
    
    initBST(&tree);
    printf("tree initiated\n");
    BSTIteratorInit(&tree, &iter);
    printf("iterator initiated\n");
    
    addBST(&tree, 10);
    addBST(&tree, 7);
    addBST(&tree, 11);
    addBST(&tree, 8);
    addBST(&tree, 9);
    addBST(&tree, 2);
    addBST(&tree, 13);
    addBST(&tree, 12);
    addBST(&tree, 23);
    addBST(&tree, 20);
    print2D(tree.root);
    
    while(BSTIteratorHasNext(&iter)!=0){
        
        printf("The top of the stack %i\n",BSTIteratorNext(&iter));
    }








    return 0;
}
*/
